import AppRouter from './router/AppRouter';

/**
 * Root application component. Renders the router which controls all page routing.
 *
 * @returns {JSX.Element} The top-level router component.
 */
function App() {
  return <AppRouter/>;
}

export default App;
